import 'package:flutter/material.dart';
import '../../../../data/mock/mock_data.dart';
class CallScreen extends StatefulWidget { const CallScreen({super.key}); @override State<CallScreen> createState() => _CallScreenState(); }
class _CallScreenState extends State<CallScreen> { bool connected = false; @override Widget build(BuildContext context) => Scaffold(backgroundColor: const Color(0xFF14213D), body: SafeArea(child: Column(children: <Widget>[const Spacer(), const CircleAvatar(radius:58, child: Icon(Icons.person, size:68)), const SizedBox(height:18), Text(MockData.advisor.name, style: const TextStyle(color:Colors.white,fontSize:24)), Text(connected ? '00:18' : 'جارٍ الاتصال...', style: const TextStyle(color:Colors.white70)), const Spacer(), Row(mainAxisAlignment: MainAxisAlignment.spaceEvenly, children: <Widget>[FloatingActionButton(onPressed: () => setState(() => connected = !connected), child: Icon(connected ? Icons.mic_off : Icons.mic)), FloatingActionButton(backgroundColor: Colors.red, onPressed: () => Navigator.pop(context), child: const Icon(Icons.call_end)), FloatingActionButton(onPressed: () {}, child: const Icon(Icons.volume_up))]), const SizedBox(height:36)]))); }
